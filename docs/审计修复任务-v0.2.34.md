# 审计修复任务 v0.2.34

> 纯描述和参数名修改，不动逻辑，不动 API 调用

---

## 修复 1：trading.ts — 整文件升级 8 字段模板

所有工具描述从老格式（一句话）升级到 8 字段模板。**注意：`okx_place_order` 的参数 `sz` 改为 `sz_adj` 或保持 `sz`，只需在描述里加中文说明。**

已完成的工具：`okx_amend_batch_orders`、`okx_get_fills_history` 已经用了 8 字段，剩下的补全。

参考格式见 `market.ts` 任意一个工具。

---

## 修复 2：algo.ts — 4 个老工具补描述

| 工具 | 当前描述 | 改为 |
|------|----------|------|
| `okx_get_algo_orders` | 一句话 | 8 字段模板 |
| `okx_get_algo_orders_history` | 一句话 | 8 字段模板 |
| `okx_place_algo_order` | 一句话 | 8 字段模板 |
| `okx_cancel_algo_order` | 一句话 | 8 字段模板 |

---

## 修复 3：public.ts — 8 个老工具补描述

| 工具 | 当前描述 |
|------|----------|
| `okx_get_mark_price` | 一句话（已有分页说明，补全其余字段） |
| `okx_get_index_price` | 一句话 |
| `okx_get_open_interest` | 一句话（已有分页说明） |
| `okx_get_system_time` | 一句话 |
| `okx_get_opt_summary` | 一句话 |
| `okx_get_insurance_fund` | 一句话 |
| `okx_convert_contract_coin` | 一句话（参数 sz 保留，加中文说明即可） |
| `okx_get_announcements` | 一句话 |

---

## 修复 4：account.ts — 1 处 sz

`okx_convert_trade` 的参数 `sz` 保持不变，只在描述里加中文说明。

---

## 模板参考

从 `src/tools/market.ts` 复制任意工具的描述格式：

```
## 功能：...
## 场景：用于...
## 关键词：xxx, xxx, xxx
## 参数：
##   - xxx: 中文说明
## 鉴权：PUBLIC / READ / TRADE / WITHDRAW
## 风险：READ / WRITE / FUND_TRANSFER / ADMIN
## 返回量：微小/中等/大量 ~X KB
## 关联：前置工具 → 本工具 → 后续工具
```

## 验证

改完跑自检，全绿提交：

```bash
npm run build
```
